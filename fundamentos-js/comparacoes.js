// == (comparação implícita)

const numero = 5;
const texto = "5";

console.log(numero == texto)

//typeof

console.log(typeof numero)

console.log (typeof texto)

// == só compara valor
//=== compara valor e o tipo de dado 

// conversão explícita

Number()
String()

// ainda há códigos que usam dois iguais (==), porém a boa prática recomenda usar apenas os três iguais (===) e converter de forma explícita caso necessite. 
